# Frontier coordinator plan

The actual coordinator for this demonstration is the Codex subagent `/root/frontier_coordinator`. I read the digital recipe instruction, approved local response registry, and initial bowl observation, then created `recipe-bowls-movement-cue-1` before the replay began. Input hashes are recorded in `planning.json`.

The local watch issues the approved visual cue when an evidenced geometric `motion_started` event identifies a bowl, with an uncalibrated score of at least 0.5 and a one-second response deadline. It expires after one hour. No persistent physical ID exists yet, so the watch matches the label; this is weaker than identity matching and is suitable only for this bounded single-seed demonstration.

The cue means “bowl movement detected; checking the step.” Pickup remains unverified. I will require chronological frames showing the same bowl group leaving its original worktop support through a lift or carry, distinguishable from camera motion, tracker drift, or occlusion. I will acknowledge exact delivered event IDs and hashes after inspecting them, and record cue readback, delivery acknowledgement, and physical completion separately.

**Current status: pending physical evidence; recipe step not complete.**
